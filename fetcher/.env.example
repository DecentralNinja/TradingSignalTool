SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Optional: WhatsApp alerts for new proven bullish/bearish signals (see
# fetcher/src/notify.js). Uses SendZen's sandbox (sendzen.io) -- sign up,
# connect the sandbox (message their sandbox number the code it gives you,
# no account-linking involved), then find SENDZEN_API_KEY and
# SENDZEN_FROM_NUMBER on the sandbox "Send Message" screen. WHATSAPP_PHONE is
# your own number without a "+", e.g. 923001234567.
WHATSAPP_PHONE=
SENDZEN_API_KEY=
SENDZEN_FROM_NUMBER=
